<view class="empty-state">
  <view class="empty-icon-wrap" wx:if="{{iconSrc}}">
    <image class="empty-icon-image" src="{{iconSrc}}" mode="aspectFit" />
  </view>
  <text class="empty-icon" wx:elif="{{icon}}">{{icon}}</text>
  <text class="empty-text">{{message}}</text>
</view>
